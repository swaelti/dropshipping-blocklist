# Dropshipping-Scraper: jeden Montag um 06:00 UTC
# Umgebungsvariablen müssen explizit übergeben werden (cron erbt sie nicht)
0 6 * * 1 root . /etc/scraper-env && /app/run.sh >> /var/log/scraper.log 2>&1

# Leere Zeile am Ende ist Pflicht für cron
